import { NoContextMenuDirective } from './no-context-menu.directive';

describe('NoContextMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new NoContextMenuDirective();
    expect(directive).toBeTruthy();
  });
});
